

on:
  push:
   
    branches:
      - master


jobs:
  build:
    runs-on: ubuntu-latest 
    steps:
      - uses: actions/checkout@v3

      - name: " login to dockerhub"
        run: docker login --username ${{ secrets.DOCKER_USERNAME1}} --password ${{ secrets.DOCKER_TOKEN }}
    

